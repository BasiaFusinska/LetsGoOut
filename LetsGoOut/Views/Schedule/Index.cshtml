@model IEnumerable<LetsGoOut.ReadModel.Activity>
@{
    ViewBag.Title = "Index";
}

<h2>Schedule</h2>
@foreach (var a in Model)
{
    @(a.IsActivity == 1 ? Html.Partial("ActivityView", a) : Html.Partial("LinkView", a))
}

@Html.ActionLink("Add", "Create")

    